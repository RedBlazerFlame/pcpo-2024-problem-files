#include "testlib.h"
using namespace std;
typedef long long ll;

const ll max_n = 300ll;

int main(int argc, char** argv) {
    registerValidation(argc, argv);
    
    ll N = inf.readLong(1, max_n, "N");
    inf.readEoln();
    inf.readEof();

    return 0;
}